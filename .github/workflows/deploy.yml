name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
  # Daily rebuild so the calendar picks up coaches' SportsYou changes even when
  # no commit lands. 13:00 UTC = 6:00 AM Arizona.
  schedule:
    - cron: "0 13 * * *"

permissions:
  id-token: write   # for OIDC role assumption
  contents: read

# Don't run two deploys against the same bucket at once. Keep them queued
# rather than cancelling so a fast follow-up commit still ships.
concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: install dependencies
        run: npm ci

      - name: build site
        run: npm run build
        env:
          # Live SportsYou calendar feed. When unset (or unreachable), the build
          # falls back to the committed snapshot at src/data/sportsyou.ics.
          SPORTSYOU_ICS_URL: ${{ secrets.SPORTSYOU_ICS_URL }}

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-west-2

      - name: sync built artifacts to S3
        run: aws s3 sync ./dist/ "s3://${{ secrets.BUCKET_NAME }}/" --delete

      - name: invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ secrets.DISTRIBUTION_ID }}" \
            --paths '/*' \
            --query 'Invalidation.{Id: Id, Status: Status}' \
            --output table
